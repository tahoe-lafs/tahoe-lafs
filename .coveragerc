# -*- mode: conf -*-

[run]
# Support concurrent test runs recording coverage information.
parallel = True

# Branch-based coverage measurements are great.  Why wouldn't you want
# information about which branches are taken?
branch = True

# only record trace data for allmydata.*
source =
       allmydata
# and don't trace the test files themselves, or generated files
omit =
     */allmydata/test/*
     */allmydata/_version.py
