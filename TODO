* Rename all the pyfec and codec layer to call the pieces "blocks" instead of "shares".
* encryption
* checking hashes
* make minimal uri by deterministic derivation of some stuff other than crypto bits and filesize
